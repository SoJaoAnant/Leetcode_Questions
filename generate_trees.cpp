#include <bits/stdc++.h>
using namespace std;

struct TreeNode
{
    int val;
    TreeNode *left;
    TreeNode *right;
};

class Solution
{
public:
    vector<TreeNode *> ans;

    TreeNode *insert(TreeNode *node, TreeNode *root)
    {
    }

    void generate()
    {

    }

    vector<TreeNode *> generateTrees(int n)
    {
        TreeNode *root;
        generate();
        return ans;
    }
};

int main()
{

    return 0;
}